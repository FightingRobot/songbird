export interface IGameState {
  score: number;
  level: number;
}

export interface ICardInfo {
  name: string;
  nameLatin: string;
  description: string;
  image: string;
  sound: string;
}